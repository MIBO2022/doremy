---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleCard from '../../components/content/ArticleCard.astro';
import { getVisiblePosts } from '../../utils/content';

const posts = await getVisiblePosts();
---
<BaseLayout title="全部文章｜Rose Lab" description="瀏覽 Rose Lab 的幼兒英文、線上外師課、免費資源與親子共學文章。" breadcrumbs={[{ name: '首頁', path: '/' }, { name: '文章', path: '/blog/' }]}>
  <main class="wrap listing-page">
    <header class="page-header"><p class="eyebrow">ROSE LAB JOURNAL</p><h1>全部文章</h1><p>依時間瀏覽幼兒英文、線上外師課、免費資源與親子共學文章。</p></header>
    <div class="article-grid">
      {posts.map((post, index) => <ArticleCard post={post} eager={index === 0} />)}
    </div>
  </main>
</BaseLayout>
